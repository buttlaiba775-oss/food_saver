<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:padding="24dp">


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Edit Post Details"
        android:textColor="#7A4F37"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <EditText
        android:id="@+id/etEditFoodTitle"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="Food name"
        android:inputType="text"
        android:background="@drawable/rounded_edittext_brown_border"
        android:paddingHorizontal="12dp"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/etEditQuantity"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="Quantity (e.g., 10 servings)"
        android:inputType="text"
        android:background="@drawable/rounded_edittext_brown_border"
        android:paddingHorizontal="12dp"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/etEditExpiryTime"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="Expiry Time (e.g., 4 Hours)"
        android:inputType="text"
        android:background="@drawable/rounded_edittext_brown_border"
        android:paddingHorizontal="12dp"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/etEditDescription"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_marginBottom="24dp"
        android:background="@drawable/rounded_edittext_brown_border"
        android:gravity="top|start"
        android:hint="Short Description..."
        android:inputType="textMultiLine"
        android:padding="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSaveChanges"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:text="Save Changes"
        android:textColor="#FFFFFF"
        app:backgroundTint="#7A4F37"
        app:cornerRadius="4dp"/>

</LinearLayout>